# Core
playwright==1.44.0
beautifulsoup4==4.12.3
httpx==0.27.0
supabase==2.4.6
anthropic==0.28.1
openai==1.35.0
pdfplumber==0.11.1
python-dotenv==1.0.1
lxml==5.2.2

# #3 — OCR per PDF scansionati
# Richiede anche: apt-get install tesseract-ocr tesseract-ocr-ita poppler-utils
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.3.0

# #7 — Alert email
# RESEND_API_KEY necessario per alert via email (resend.com — piano free: 3k email/mese)
# httpx già incluso sopra

# GitHub Actions: aggiungere questo step prima di pip install:
# - run: sudo apt-get install -y tesseract-ocr tesseract-ocr-ita poppler-utils
